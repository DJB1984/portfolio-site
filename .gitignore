# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# local tooling state (Claude Code / Impeccable) — not part of the site
/.claude/
/.impeccable/config.local.json
/.impeccable/hook.cache.json

# personal working files (kept local, not published)
/notes.md
/intial references/

# hostgator deploy — account/host-specific, never committed
/deploy.config.json

# typescript
*.tsbuildinfo
next-env.d.ts
