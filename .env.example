# Copy this file to `.env.local` (which is git-ignored) and set a password.
# This password unlocks the private, LOCAL-ONLY in-browser edit mode at /unlock.
# Editing only works while running `npm run dev` — it is disabled in production.
EDIT_PASSWORD=change-me
